{
  "name": "kimi-code-workbench",
  "version": "1.0.0-beta.2",
  "description": "Unofficial local desktop workbench for Kimi Code with quota forecasting, visual configuration, artifact diffs, file previews, and task time travel",
  "main": "src/main/main.js",
  "type": "module",
  "author": "Dylan5237",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "demo": "electron . --demo",
    "test": "node --test",
    "build": "electron-builder --win --dir",
    "dist": "electron-builder --win",
    "pack": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.0.12"
  },
  "build": {
    "appId": "com.local.kimicodeworkbench",
    "productName": "KimiCode Workbench",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json",
      "README.md"
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "KimiCode-Workbench-${version}-${arch}.${ext}"
    }
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "marked": "^12.0.2",
    "mermaid": "^11.16.0"
  }
}
